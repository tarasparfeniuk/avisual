export const demo = {
    minSpeed: 1,
    maxSpeed: 100,
    stepInterval: 500
};

export const array = {
    minElement: -10,
    maxElement: 10,
    minSize: 2,
    maxSize: 250
};

Object.freeze(array);
Object.freeze(demo);
